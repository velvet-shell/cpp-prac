void process(const std::vector<int> &v1, std::vector<int> &v2)
{
	std::vector ind = v1;

	// std::unique(ind.begin(), ind.end());
	// std::sort(ind.begin(), ind.end());


	for (const auto &x : ind) {
					
	}
}